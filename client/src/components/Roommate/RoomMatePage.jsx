import React from 'react'

const RoomMatePage = () => {
    return (
        // Send the GET request to /api/flat to get all flat-requests
        <div>RoomMatePage</div>
    )
}

export default RoomMatePage